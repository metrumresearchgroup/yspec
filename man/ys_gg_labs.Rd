% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ys-gg-labs.R
\name{ys_gg_labs}
\alias{ys_gg_labs}
\title{Automatically label ggplot aesthetics from a yspec object or other source}
\usage{
ys_gg_labs(
  spec = NULL,
  labs = list(),
  x = NULL,
  y = NULL,
  fill = NULL,
  colour = NULL,
  color = NULL,
  col = NULL,
  linetype = NULL,
  lty = NULL,
  shape = NULL,
  warn = TRUE,
  ...
)
}
\arguments{
\item{spec}{a yspec object; label data is generated through a call to
\code{\link[=ys_get_short_unit]{ys_get_short_unit()}}.}

\item{labs}{a named list of label data; names correspond to columns
in the data used to make the plot; overrides \code{spec}.}

\item{x}{label for the x aesthetic. If \code{NULL}, resolved via the mapped
column name. Pass a column name as a plain string to look it up in \code{spec} or
\code{labs}; wrap in \code{\link[=I]{I()}} to use the string as a literal label.}

\item{y}{label for the y aesthetic; see \code{x}.}

\item{fill}{label for the fill aesthetic; see \code{x}.}

\item{colour, color, col}{label for the colour aesthetic; see \code{x}.}

\item{linetype, lty}{label for the linetype aesthetic; see \code{x}.}

\item{shape}{label for the shape aesthetic; see \code{x}.}

\item{warn}{if \code{TRUE} (default), warn when the same aesthetic is mapped to
multiple variables that each have a spec entry but resolve to different
labels.}

\item{...}{additional arguments passed to \code{\link[ggplot2:labs]{ggplot2::labs()}}.}
}
\value{
A gg object that can be added to a ggplot with \code{+}.
}
\description{
Automatically label ggplot aesthetics from a yspec object or other source
}
\examples{
if(requireNamespace("ggplot2")) {
  library(ggplot2)
  
  spec <- ys_help$spec()

  spec <- update_short(spec, TIME = "Time")
  
  data <- ys_help$data()
  
  p <- ggplot(data, aes(TIME, DV)) + geom_point()
  
  p + ys_gg_labs(spec)
}

}
